<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--关闭启动窗口-->
        <item name="android:windowDisablePreview">true</item>

    </style>

    //im plus
    <style name="MyPopupMenuStyle" parent="Widget.AppCompat.PopupMenu">
        <item name="android:popupMenuStyle">@style/MyPopupMenuStyle2</item>
        <item name="android:dropDownListViewStyle">@style/MyPopupMenuDivider</item>
        <item name="android:textAppearanceSmallPopupMenu">@style/MyPopupMenuText</item>
        <item name="android:textAppearanceLargePopupMenu">@style/MyPopupMenuText</item>
    </style>

    <style name="MyPopupMenuStyle2" parent="@android:style/Widget.PopupMenu">
        <item name="android:popupBackground">@drawable/ic_popup_menu</item>
    </style>

    <style name="MyPopupMenuText">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">14sp</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="MyPopupMenuDivider">
        <item name="android:divider">@color/light_gray2</item>
        <item name="android:dividerHeight">1dp</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:paddingRight">10dp</item>
    </style>


    <style name="MyEditTextStyle">
        <item name="android:textColorHint">@color/light_gray3</item>
        <item name="colorControlNormal">@color/light_gray3</item>
        <item name="colorControlActivated">@color/middle_gray</item>
    </style>
</resources>
